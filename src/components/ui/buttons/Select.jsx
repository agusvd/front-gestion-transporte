import React from 'react'

const Select = (props) => {
    return (
        <select className='block p-2 bg-blue-500 text-white rounded-xl'
        {...props}>
        </select>
    )
}

export default Select