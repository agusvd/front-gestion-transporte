import React from 'react'

const ButtonNo = (props) => {
    return (
        <button className='block bg-red-500 hover:bg-red-700 rounded-xl duration-500 ease-in-out text-white p-2 text-md font-primary' 
        {...props}>
        </button>
    )
}

export default ButtonNo